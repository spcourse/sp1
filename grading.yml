grades:
    algorithms:
        type: pass
        subgrade:
            checkpy: integer
        automatic:
            checkpy: correctness_score
        calculation: correctness_score < 13 && 0 || -1

    numbers:
        type: pass
        subgrade:
            checkpy: integer
        automatic:
            checkpy: correctness_score
        calculation: correctness_score < 22 && 0 || -1
