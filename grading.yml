grades:
    algorithms:
        type: pass
        subgrades:
            checkpy: integer
        automatic:
            checkpy: correctness_score
        calculation: checkpy < 13 && 0 || -1
        auto_publish: true

    numbers:
        type: pass
        subgrades:
            checkpy: integer
        automatic:
            checkpy: correctness_score
        calculation: checkpy < 22 && 0 || -1
        auto_publish: true
